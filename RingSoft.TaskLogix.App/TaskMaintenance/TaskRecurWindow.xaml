<dec:BaseWindow x:Class="RingSoft.TaskLogix.App.TaskMaintenance.TaskRecurWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:dec="clr-namespace:RingSoft.DataEntryControls.WPF;assembly=RingSoft.DataEntryControls.WPF"
        xmlns:ac="clr-namespace:RingSoft.App.Controls;assembly=RingSoft.App.Controls"
        xmlns:da="clr-namespace:RingSoft.TaskLogix.DataAccess.Model;assembly=RingSoft.TaskLogix.DataAccess"
        xmlns:vm="clr-namespace:RingSoft.TaskLogix.Library.ViewModels;assembly=RingSoft.TaskLogix.Library"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RingSoft.TaskLogix.App.TaskMaintenance"
        mc:Ignorable="d"
        Title="TaskRecurWindow" Height="450" Width="800"
        SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:TaskRecurWindowViewModel x:Name="LocalViewModel"/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Recurrence pattern">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical">

                    <RadioButton x:Name="DailyRadio" Margin="5" Content="Daily" IsChecked="{Binding Path=RecurType, Converter={StaticResource ComparisonConverter}, ConverterParameter={x:Static da:TaskRecurTypes.Daily}, Mode=TwoWay}"/>

                    <RadioButton x:Name="WeeklyRadio" Margin="5" Content="Weekly" IsChecked="{Binding Path=RecurType, Converter={StaticResource ComparisonConverter}, ConverterParameter={x:Static da:TaskRecurTypes.Weekly}, Mode=TwoWay}"/>

                    <RadioButton x:Name="MonthlyRadio" Margin="5" Content="Monthly" IsChecked="{Binding Path=RecurType, Converter={StaticResource ComparisonConverter}, ConverterParameter={x:Static da:TaskRecurTypes.Monthly}, Mode=TwoWay}"/>

                    <RadioButton x:Name="YearlyRadio" Margin="5" Content="Yearly" IsChecked="{Binding Path=RecurType, Converter={StaticResource ComparisonConverter}, ConverterParameter={x:Static da:TaskRecurTypes.Yearly}, Mode=TwoWay}"/>

                </StackPanel>

                <Border Grid.Row="0" Grid.Column="1" BorderThickness="1" BorderBrush="White"/>

                <StackPanel x:Name="RecurPanel" Grid.Row="0" Grid.Column="2" Width="300" Height="300"/>
            </Grid>
        </GroupBox>
    </Grid>
</dec:BaseWindow>
