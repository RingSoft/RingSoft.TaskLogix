<lc:DbMaintenanceUserControl x:Class="RingSoft.TaskLogix.App.TaskMaintenance.TaskHistoryUserControl"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                             xmlns:dec="clr-namespace:RingSoft.DataEntryControls.WPF;assembly=RingSoft.DataEntryControls.WPF"
                             xmlns:lc="clr-namespace:RingSoft.DbLookup.Controls.WPF;assembly=RingSoft.DbLookup.Controls.WPF"
                             xmlns:ac="clr-namespace:RingSoft.App.Controls;assembly=RingSoft.App.Controls"
                             xmlns:vm="clr-namespace:RingSoft.TaskLogix.Library.ViewModels;assembly=RingSoft.TaskLogix.Library"
                             xmlns:local="clr-namespace:RingSoft.TaskLogix.App.TaskMaintenance"
                             mc:Ignorable="d" 
                             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:TaskHistoryViewModel x:Name="LocalViewModel"/>
    </UserControl.DataContext>

    <DockPanel>
        <ac:DbMaintenanceTopHeaderControl x:Name="TopHeaderControl" DockPanel.Dock="Top"/>
        <lc:DbMaintenanceStatusBar x:Name="StatusBar" DockPanel.Dock="Bottom"/>

        <Grid Margin="5">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="History Database ID"/>

            <dec:StringReadOnlyBox Grid.Row="0" Grid.Column="1" Width="75" HorizontalAlignment="Left" Text="{Binding Id, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="0" Grid.Column="2" Content="Task"/>

            <lc:AutoFillReadOnlyControl Grid.Row="0" Grid.Column="3" Width="200" HorizontalAlignment="Left" Setup="{Binding TaskAutoFillSetup}" Value="{Binding TaskAutoFillValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Start Date"/>

            <dec:DateReadOnlyBox Grid.Row="1" Grid.Column="1" Width="100" DateFormatType="DateOnly" HorizontalAlignment="Left"/>

            <Label Grid.Row="1" Grid.Column="2" Content="Due Date"/>

            <dec:DateReadOnlyBox Grid.Row="1" Grid.Column="3" Width="100" DateFormatType="DateOnly" HorizontalAlignment="Left"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Completion Date"/>

            <dec:DateReadOnlyBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="4" Width="175" DateFormatType="DateTime" HorizontalAlignment="Left"/>


        </Grid>

    </DockPanel>
</lc:DbMaintenanceUserControl>
